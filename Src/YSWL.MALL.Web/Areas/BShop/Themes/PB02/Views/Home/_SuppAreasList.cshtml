@model List<YSWL.MALL.Model.Shop.Supplier.SuppAreas>
<ul class="tab-tit clearfix">
    @if (Model != null)
    {
        for (int i = 0; i < Model.Count; i++)
        {
            <li><a @(i==0?"class=tab-active":"") href="javascript:;">@Model[i].Name</a></li>
        }
    }
</ul>
