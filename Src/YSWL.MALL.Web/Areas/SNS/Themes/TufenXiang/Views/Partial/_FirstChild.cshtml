@model List<YSWL.MALL.Model.SNS.Categories>
@if (Model != null && Model.Count > 0)
{
    <ul>
        @foreach (var item in Model)
        {
            <li><a href="@(ViewBag.BasePath)Photo/Index/-1/@item.CategoryId/all/hot">@item.Name</a></li>
        }
    </ul>

}