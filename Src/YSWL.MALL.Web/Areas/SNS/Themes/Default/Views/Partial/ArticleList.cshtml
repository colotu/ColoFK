@{
    Layout = null;
}
@model List<YSWL.MALL.Model.CMS.Content>
@if (null != Model)
{
    foreach (var item in Model)
    {
    <li><a href="@(ViewBag.BasePath)Article/Details/@item.ContentID">@item.Title</a></li>
    }
}