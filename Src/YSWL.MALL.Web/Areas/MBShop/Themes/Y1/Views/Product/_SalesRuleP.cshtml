@using Webdiyer.WebControls.Mvc
<script type="text/javascript">
                       $(function () {
                           waterFall.init({
                               //Ajax请求参数
                               AjaxOptions: {
                                   //Ajax请求参数
                                   Params: { ajaxViewName: "_SalesRulePWaterfall" },
                                   //Params:@Html.Raw(ViewBag.DataParam),
                                   //Ajax请求URL
                                   DataURL: "@(ViewBag.BasePath)Product/SalesRuleP"
                               },
                               //瀑布流起始索引记录器 - 用于累计
                               CurrentAjaxStartIndexHF: $('#hfCurrentPageAjaxStartIndex'),
                               //瀑布流起始索引
                               StartIndex:$('#hfCurrentPageAjaxStartIndex').val() ? parseInt($('#hfCurrentPageAjaxStartIndex').val()) : 0,
                               //瀑布流结束索引
                               EndIndex: $('#hfCurrentPageAjaxEndIndex').val() ? parseInt($('#hfCurrentPageAjaxEndIndex').val()) : 0,

                               //分页容器
                               PagedContainer: $('.in_pages'),
                               //列容器ID前缀
                               ColumnFirstId: "J_lazyload",
                               //列数
                               ColumnNumber: 0,RowNumber: 3
                           });
                           $.lazyFixedLoadImg('.prod_img', 158, 158);
                       });
</script>

@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
        <div class="recentorder wrap">
            <div class="recent_prod">
                <div class="cart_photo_wrap">
                    <a href="@(ViewBag.BasePath)p/d/@item.ProductId" class="cart_photo_cell">
                        <img class="shp_cart_img" src="@YSWL.Web.Components.FileHelper.GeThumbImage(item.ThumbnailsUrl1, "T81X81_")" alt="">
                    </a>
                    <div class="prod_tip bg_red">
                        打折
                    </div>
                </div>
                <div class="recent_text">
                    <span class="text_span">PS2123152704</span>
                    <p>@item.ProductName</p>
                    <span class="text_span">规格 6.18L</span>
                </div>
            </div>
            <div class="order_price">
                <p class="price_red">￥@item.LowestSalePrice.ToString("f")</p>
                @*<div class="cart icon" onclick="dialogShow(@item.ProductId,@item.SupplierId,@item.ThumbnailsUrl)">
                    <span></span>
                </div>*@

                @{
                    if (item.SalesType == 1 && item.SaleStatus == 1 && ViewBag.IsMultiDepot)
                    {
                        <div class="cart icon" onclick="dialogShow(@item.ProductId, @item.SupplierId, '@item.ThumbnailUrl1',@item.SupplierId,@item.ProductId)">
                            <span></span>
                        </div>
                    }
                    else
                    {
                        <div class="cart icon" onclick="dialogShow(@item.ProductId,@item.SupplierId,@item.ThumbnailsUrl)">
                            <span></span>
                        </div>
                    }
                }
            </div>
        </div>
    }
}

@*@if (Model != null && Model.ProductList.Count > 0)
{
    <div class="recentorder wrap">
        <div class="recent_prod">
            <div class="cart_photo_wrap">
                <a href="#" class="cart_photo_cell">
                    <img class="shp_cart_img" src="images/6.jpg" alt="">
                </a>
                <div class="prod_tip bg_red">
                    打折
                </div>
            </div>
            <div class="recent_text">
                <span class="text_span">PS2123152704</span>
                <p>鲁花5s压榨一级花生油6.18L</p>
                <span class="text_span">规格 6.18L</span>
            </div>
        </div>
        <div class="order_price">
            <p class="price_red">￥ 160.00元</p>
            <div class="cart icon">
                <span></span>
            </div>
        </div>
    </div>

    for (int i = 0; i < Model.ProductList.Count; i++)
    {
        <div class="prod col-xs-6 col-sm-3 product">
            <a class="prod_img" href="@(ViewBag.BasePath)p/d/@Model.ProductList[i].ProductId" target="_blank">
                <img src="@YSWL.Web.Components.FileHelper.GeThumbImage(Model.ProductList[i].ThumbnailUrl1, "T158X158_")" alt="">
            </a>
            <div class="prod_item">
                <span>@Model.ProductList[i].ProductName</span>
                <span class="price_red">￥@Model.ProductList[i].LowestSalePrice.ToString("f")</span>
            </div>
            <div class="cart icon" onclick="dialogShow(@Model.ProductList[i].ProductId,@Model.ProductList[i].SupplierId,'@Model.ProductList[i].ThumbnailUrl1');">
                <span></span>
            </div>
        </div>
    }
    <input type="hidden" id="hfCurrentPageAjaxStartIndex" value="@ViewBag.CurrentPageAjaxStartIndex" />
                <input type="hidden" id="hfCurrentPageAjaxEndIndex" value="@ViewBag.CurrentPageAjaxEndIndex" />
                <input type="hidden" id="hfCurrentPageAjaxSize" value="@ViewBag.CurrentPageAjaxSize" />
}*@
