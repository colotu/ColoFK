@model List<YSWL.MALL.Model.Shop.Products.CategoryInfo>
@if (Model != null && Model.Count > 0)
{
   
        for (int i = 0; i < Model.Count; i++)
        {
            <dd>
    <a href="@(ViewBag.BasePath)product/@Model[i].CategoryId" target="_blank" title="@Model[i].Name" >@YSWL.Common.StringPlus.SubString(Model[i].Name, 12, "...")</a>
    </dd>
        }
   }