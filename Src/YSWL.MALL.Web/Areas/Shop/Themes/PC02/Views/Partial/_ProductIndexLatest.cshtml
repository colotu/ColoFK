@model List<YSWL.MALL.Model.Shop.Products.ProductInfo> 
    <div class="promotionGoodsCont">
         @if (Model != null && Model.Count > 0)
         {
             foreach (var item in Model)
             {
                 <dl>
                     <dt><a href="@item.SeoUrl"><img width="60" height="60" alt="@item.ProductName" 
                             src="@YSWL.MALL.Web.Components.FileHelper.GeThumbImage(item.ThumbnailUrl1, "T115X115_")"></a></dt>
                     <dd><a style="height: 49px;overflow: hidden;display: block;" href="@item.SeoUrl">@item.ProductName</a></dd>
                     <dd class="red">￥@item.LowestSalePrice.ToString("F")</dd>
                 </dl>

             }
         }
            </div>
