@model List<YSWL.MALL.Model.Settings.Advertisement>

    @if (Model != null)
    {
        for (int i = 0; i < Model.Count; i++)
        {
        <li class=""><a href="javascript:;" class="rslides1_s1"></a></li>
        }
    }

