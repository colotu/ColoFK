@model List<YSWL.MALL.Model.Shop.Products.CategoryInfo>
 @if (Model != null && Model.Count > 0)
 {
     <div class="prdsort-item">
         <ul>
             @foreach (var item in Model)
             {
                    <li><a href="@(ViewBag.BasePath)product/@item.CategoryId"
                    target="_blank">@item.Name</a></li>
             }
          
         </ul>
                </div>
 }

