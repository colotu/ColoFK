@model List<YSWL.MALL.Model.Shop.Products.CategoryInfo>
@if (Model != null && Model.Count > 0)
{
    foreach (var item in Model)
    {
    <dd>
        <a target="_blank" href="@(ViewBag.BasePath)product/@item.CategoryId"  
            >@item.Name</a></dd>  
    }
}